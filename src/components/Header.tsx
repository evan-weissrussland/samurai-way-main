import React from "react";
import image1 from '../image1.svg'

export const Header = () => {
    return (
        <header className={'header'}>
                <img src={image1} alt={''}/>
            </header>
    );
}
